# .env.example — copy to .env and fill in real values.
# This file (the .example one) is safe to commit; .env itself is not
# — it's already excluded in .gitignore.

GITHUB_TOKEN=
GITHUB_OWNER=jass666
GITHUB_REPO=jaswant-kanojia
GITHUB_BRANCH=main
ADMIN_EMAIL=jaswantkanojia4@gmail.com
